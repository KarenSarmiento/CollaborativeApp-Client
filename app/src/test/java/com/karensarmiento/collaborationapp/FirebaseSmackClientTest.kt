package com.karensarmiento.collaborationapp

import org.junit.Assert
import org.junit.Test

class FirebaseSmackClientTest {
    @Test
    fun addition_isCorrect() {
        Assert.assertEquals(4, 2 + 2)
    }
}